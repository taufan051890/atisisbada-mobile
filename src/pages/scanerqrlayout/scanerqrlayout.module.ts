import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ScanerqrlayoutPage } from './scanerqrlayout';

@NgModule({
  declarations: [
    ScanerqrlayoutPage,
  ],
  imports: [
    IonicPageModule.forChild(ScanerqrlayoutPage),
  ],
})
export class ScanerqrlayoutPageModule {}
